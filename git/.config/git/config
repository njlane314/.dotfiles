[core]
	pager = ~/.config/git/pager

[interactive]
	diffFilter = ~/.config/git/pager --color-only

[delta]
	navigate = true
	side-by-side = true
	line-numbers = true

[merge]
	conflictstyle = diff3

[diff]
	colorMoved = default

[alias]
	s = status --short --branch
	co = switch
	cob = switch -c
	br = branch
	ci = commit
	amend = commit --amend --no-edit
	lg = log --oneline --decorate --graph --all
	unstage = restore --staged
	undo = reset --soft HEAD~1
