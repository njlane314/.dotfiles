#!/usr/bin/env bash
set -euo pipefail

if command -v delta >/dev/null 2>&1; then
  exec delta "$@"
fi

if [[ "${1:-}" == "--color-only" ]]; then
  exec cat
fi

if command -v less >/dev/null 2>&1; then
  exec less -FRX
fi

exec cat
