name: Check

on:
  pull_request:
  push:

jobs:
  check:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install packages
        run: brew install stow vim emacs tmux llvm
      - name: Run checks
        run: make check
